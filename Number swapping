//program to swap 2 numbers without using temp variable 
#include<stdio.h>
int main()
{
    int a,b;
    printf("Enter the numbers:");
    scanf("%d%d",&a,&b);
    printf("Entered numbers are: a=%d b=%d\n",a,b);
    a=a+b;
    b=a-b;
    a=a-b;
    printf("After swapping: a=%d b=%d",a,b);
}
    
