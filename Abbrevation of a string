#include<stdio.h>
#include<string.h>
int main()
{
  char A[50];
  int i=0;
  printf("Enter the string:");
  gets(A);
  strupr(A);
  printf("%c",A[0]);
   while(A[i])
   {
    if(A[i]==' ')
      {
      printf(" %c",A[i+1]);
      }
     i++;
   }
}
