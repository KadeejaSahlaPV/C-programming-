#include <stdio.h>
int main() 
{
 int n,i,j,k;
 printf("Enter the limit :");
 scanf("%d",&n);
  for(i=0;i<n;i++)
  {
    printf("\n");
      for(j=1;j<n-i;j++)
      {
        printf(" ");
       }
        for(k=0;k<=i;k++)
          printf(" * ");
   }
 }   
