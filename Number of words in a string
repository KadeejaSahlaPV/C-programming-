#include<stdio.h>
#include<string.h>
int main() 
{ 
  int i,count=1; 
  char a[100];
  printf("Enter the string:"); 
  gets(a);
   for(i=0;a[i]!='\0';i++) 
    {
     if(a[i]==' '||a[i]=='\n'||a[i]=='\t')
     count++;
    }
  printf("Total number of words:%d",count);
}
